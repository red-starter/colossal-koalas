// Your .env.json should match the shape of the example object below.
{
  "PGSSLMODE" : "require", // This should not be changed, to work around a Sequelize issue
  "DATABASE_URL": "your_postgres_server_url",
  "TOKEN_SECRET": "your_chosen_secret"
}